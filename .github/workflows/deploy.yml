name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
    paths: [ 'frontend/**', '.github/workflows/deploy.yml' ]
  pull_request:
    branches: [ main ]
    paths: [ 'frontend/**', '.github/workflows/deploy.yml' ]
  # Dependabot PR の自動マージ後に dependabot-auto-merge.yml から起動される。
  # GITHUB_TOKEN による push は workflow を起動しないため、この入口が必要。
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Setup Node.js
      uses: actions/setup-node@v7
      with:
        node-version: '24'
        cache: 'npm'
        cache-dependency-path: 'frontend/package-lock.json'

    - name: Install dependencies
      run: npm ci
      working-directory: ./frontend

    - name: Set environment variables
      run: |
        echo "NEXT_PUBLIC_BASE_PATH=/Echoes_in_the_Tide" >> .env.local
      working-directory: ./frontend

    - name: Build
      run: npm run build
      working-directory: ./frontend
      env:
        NODE_ENV: production

    - name: Add .nojekyll file
      run: touch ./frontend/out/.nojekyll

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./frontend/out
        publish_branch: gh-pages
        force_orphan: true
